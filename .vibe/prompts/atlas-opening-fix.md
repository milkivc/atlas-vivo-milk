Tu és o implementador de UMA única correcção da abertura pública do Atlas Vivo MILK.

Lê somente:
1. `docs/agent-input/OPENING_CANONICAL_LOCK.md`
2. `deploy/atlas-public/experience-machine.js`
3. `deploy/atlas-public/index.html`
4. `deploy/atlas-public/atlas.js`
5. `deploy/atlas-public/styles.css` apenas se precisares de estilo.

Faz somente isto:
- materializa PRETO → SELO → TOQUE antes do COSMICOXES existente;
- começa em PRETO;
- o Selo Atlas já existente (`assets/selo-atlas.png`) aparece como limiar discreto e acessível;
- pointer/touch/Enter/Espaço no selo fazem o selo desaparecer e entram no COSMICOXES existente;
- não alteres conteúdo, texto, catálogo, curadorias, território, festas, assets ou a progressão posterior;
- preserva `prefers-reduced-motion`;
- não uses rede nem outra ferramenta além de leitura/edição/teste local;
- altera no máximo 4 ficheiros;
- executa `node --check` nos JS alterados, `node --test tests/ia-milk-public-boundary.test.mjs` e `git diff --check`;
- quando os testes terminarem, responde final e PARA. Não continues a explorar o projecto.
