extends: default

rules:
  line-length:
    max: 120
    level: warning
  trailing-spaces:
    level: error
  indentation:
    spaces: 2
    indent-sequences: consistent
  empty-lines:
    max: 2
    max-start: 0
    max-end: 1
