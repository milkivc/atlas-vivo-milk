name: Mistral official Drive to PTServidor migration v3

on:
  push:
    branches: [codex/safety-gate-2026-08-10]
    paths:
      - 'ops/mistral_official_migration_v3.py'
      - '.github/workflows/mistral-official-migration-v3.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/safety-gate-2026-08-10
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Execute Mistral official migration v3
        run: python ops/mistral_official_migration_v3.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: atlas-official-migration-v3-receipt
          path: artifacts/migration-official-v3/receipt.json
          if-no-files-found: error
