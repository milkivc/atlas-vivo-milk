name: MILK COPERNICO — historical Git probe (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'GIT_ONLY_RECOVERY_RETIRED=1'
          echo 'Reason: full COPERNICO source was recovered from the canonical Drive corpus; WebApp work continues in the EU-source integration workflow.'
          echo 'PUBLICATION=NO'
