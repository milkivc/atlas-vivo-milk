name: MILK Mistral Canonical Opening Fix (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Resolver handoff
        run: |
          echo 'STATUS=RETIRED'
          echo 'ACTIVE_RESOLVER=.github/workflows/mistral-blocker-resolver.yml'
          echo 'PRODUCTION_PUBLISH=NO'
