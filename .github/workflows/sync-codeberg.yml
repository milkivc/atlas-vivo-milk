name: DEPRECATED — unsafe Codeberg mirror blocked

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Stop legacy full mirror
        run: |
          echo "BLOCKED: legacy git push --mirror --force workflow is intentionally disabled."
          echo "Use mistral-research-platform-corridor.yml for sanitised allowlist migration to the canonical Codeberg/Forgejo repository."
          exit 1
