name: ARCHIVED — legacy Codeberg sync

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical route
        run: |
          echo "This duplicate workflow is intentionally inactive."
          echo "Use: MILK Mistral — GitHub to Codeberg (safe sync)."
          echo "No repository or remote was changed."
